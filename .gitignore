# System Files
.DS_Store
Thumbs.db
*.log
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Build and Dependency Directories
node_modules/
/build/
/.svelte-kit/
/package/

# Local Environment Variables
.env
.env.local
.env.[a-zA-Z0-9]*
!.env.example

# Playwright/Cypress Testing Artifacts
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# IDEs and Editors
.idea/
.vscode

# Data directory (DB + file storage), and archives taken of it. Both hold real
# books and receipts copied from the live system — never track either.
/data/
akaun_data.zip
.vercel
.superpowers/
__MACOSX

# tesseract.js OCR language caches — downloaded at runtime into the worker's
# cachePath (see src/lib/server/extraction/document-text.ts). Older versions of this
# repo accidentally committed these from the CWD; never track them.
*.traineddata
